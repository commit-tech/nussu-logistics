<div class="list">
  <h1>Items List</h1>

  <div class="container">
    <div class="row">
      <div class="col-md-12 scrollable">

        <table>
          <tr>
            <th class="name">Name</th>
            <th class="desc">Description</th>
            <th class="quantity">Quantity</th>
            <th class="more">More details</th>
            <th class="book">Book</th>

          </tr>
          <% @items.each do |i| %>
            <tr>
              <td class="name"><%= i.name %></td>
              <td class="desc"><%= truncate(i.description, :length => 30) %></td>
              <td class="quantity"><%= i.quantity %></td>
              <td class="more"><%= link_to 'Show Full Details', item_path(i) %></td>
              <td class="book"><%= button_to("Book item", items_path,
                method: "get", class: "book-btn btn-success") %></td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</div>
